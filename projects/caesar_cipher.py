#EH 1st caesar cipher
import re
upper = r"[A-Z]"
lower = r"[a-z]"
while True:
    def code(text, shift, bruh):

    #user input to see if they would like to 
    bruh = input("Would you like to decode a message, or encode a message with the caesar cipher?\n")
    if bruh == "decode":
        code()
    elif bruh == "encode":
        code()
    else:
        print("Not a valid input buddy")