#EHCP1 factorial calculator
#for num in num
#def factorial(num-=1)
#if num == 1
#print(f"{num} is the og, and {fac} is the factorial)
#added try and except to idiot proof
try:
    number = int(input("What is the number that that you want to check the factorial of\n"))
except ValueError:
    print("")
def fac(factorial):
    while True:
        factorial = (number * number - 1)
        if number == 1:
            break
print(factorial)